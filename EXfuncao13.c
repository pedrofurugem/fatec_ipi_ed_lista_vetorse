#include<stdio.h>

//Escreva uma fun̤̣o que recebe um vetor de char v e um char c. A fun̤̣o deve
//substituir cada ocorr̻ncia de c em v por *

void troca_v (char vet[v], char vet[c]){
    int tamV, tamC;
    for(tamV=0; vet[v]; tamV++){
        for(tamC=0; vet[c]; tamC=+){
       }
    }
    if(vet[v] == v){
        return c;
    }
    else{
        return v;
    }

}

int main(void){
    char vet[ch], vet[vh];
    int tam;
    for(tam=0; vet[v]; tam++){
    printf("informe charateres ou palavras \n");
    scanf("%c", char(vet[v]));
    scanf("%c", char(vet[c]));
    }
    
    return 0;
}